'use strict';

describe('myApp.notes module', function() {

  beforeEach(module('myApp.notes'));

  describe('notes controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var notesCtrl = $controller('notesCtrl');
      expect(notesCtrl).toBeDefined();
    }));

  });
});
